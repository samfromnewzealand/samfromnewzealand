---
layout: base.njk
title: Home
---

<section class="hero">
  <h1>Kia Ora! I'm Sam from New Zealand</h1>
  <p class="lead">Welcome to my corner of the web where I share stories, photos, and adventures from Aotearoa.</p>
  <a href="/posts/" class="button">Read My Posts</a>
</section>

<section class="recent-posts">
  <h2>Latest Posts</h2>
  <div class="post-grid">
    {% for post in collections.post | reverse | slice(3) %}
      <div class="post-card">
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <time datetime="{{ post.date | w3DateFilter }}">{{ post.date | dateDisplay }}</time>
        <p>{{ post.data.excerpt or post.templateContent | striptags | truncate(100) }}</p>
        <a href="{{ post.url }}" class="read-more">Continue reading →</a>
      </div>
    {% endfor %}
  </div>
  <div class="view-all">
    <a href="/posts/" class="button">View All Posts</a>
  </div>
</section>

<section id="subscription-form" class="subscription">
  <h2>Subscribe to My Newsletter</h2>
  <p>Get updates on my latest adventures and posts directly to your inbox.</p>
  <form class="email-form">
    <div class="form-group">
      <label for="email-subscribe">Email Address</label>
      <input type="email" id="email-subscribe" placeholder="your@email.com" required>
    </div>
    <button type="submit" class="button">Subscribe</button>
  </form>
</section>
