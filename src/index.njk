---
layout: base
title: Home
description: Personal website of Sam from New Zealand
---

<div class="hero">
  <div class="oval-logo">
    <img src="/img/sam.svg" alt="Sam from NZ" class="oval-image">
  </div>
  
  <h1>Hi, I'm Sam.</h1>
  <p class="lead">Let's talk technical marketing and data.</p>

</div>

<section class="recent-posts">
  <h2>Recent Articles</h2>
  
  <div class="post-grid">
    {% set recentPosts = collections.post | reverse | slice(3) %}
    
    {% for post in recentPosts %}
    <article class="post-card">
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <time datetime="{{ post.date.toISOString() }}">{{ post.date | dateDisplay }}</time>
      
      {% if post.data.description %}
      <p>{{ post.data.description }}</p>
      {% endif %}
      
      <a href="{{ post.url }}" class="read-more">Continue reading →</a>
    </article>
    {% endfor %}
    
    {% if recentPosts.length === 0 %}
    <p class="no-posts">No posts published yet. Check back soon!</p>
    {% endif %}
  </div>
  
  <div class="view-all">
    <a href="/blog/" class="read-more">View all posts →</a>
  </div>
</section>

<section class="subscription">
  <h2>Stay Updated</h2>
  <p>Subscribe to get notified when I publish new articles.</p>
  
  <form class="email-form" action="/subscribe/" method="post">
    <div class="form-group">
      <label for="email">Email Address</label>
      <input type="email" id="email" name="email" placeholder="your@email.com" required>
    </div>
    
    <button type="submit" class="button">Subscribe</button>
  </form>
  
  <p class="small">I'll never share your email with anyone else.</p>
</section>
